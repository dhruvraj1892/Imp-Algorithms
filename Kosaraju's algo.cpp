//used to find the count of strongly connected components in a directed graph which may contains cycles 
// first topo sort the graph using stack and dfs
// reverse all the edges of the graph  
// start with the top-most vertex in the stack and run dfs and count the components accordingly


